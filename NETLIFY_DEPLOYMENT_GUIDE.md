# 🚀 Инструкция по деплою на Netlify

Этот проект состоит из фронтенда (React) и бэкенда (FastAPI). Для полного функционирования нужно задеплоить оба компонента.

## 📋 Предварительные требования

1. **Аккаунты на платформах:**
   - Netlify (для фронтенда)
   - Railway/Render/Heroku (для бэкенда)
   - Supabase/Neon (для PostgreSQL)
   - Upstash/Redis Cloud (для Redis)

2. **API ключи:**
   - OpenAI API key
   - Telegram Bot Token
   - Twitter API keys (опционально)
   - GitHub Token (опционально)
   - Reddit API keys (опционально)
   - SendGrid API key (опционально)

## 🎯 Пошаговая инструкция

### Шаг 1: Подготовка базы данных

1. **Создайте PostgreSQL базу данных:**
   - Supabase: https://supabase.com
   - Neon: https://neon.tech
   - Railway PostgreSQL: https://railway.app

2. **Создайте Redis:**
   - Upstash: https://upstash.com
   - Redis Cloud: https://redis.com
   - Railway Redis: https://railway.app

### Шаг 2: Деплой бэкенда

Выберите одну из платформ:

#### Вариант A: Railway.app (рекомендуется)

1. Подключите GitHub репозиторий к Railway
2. Создайте новый проект
3. Добавьте PostgreSQL и Redis сервисы
4. Деплойте бэкенд из папки `backend/`
5. Настройте переменные окружения (см. `DEPLOYMENT_ENV_VARS.md`)

#### Вариант B: Render.com

1. Подключите GitHub репозиторий к Render
2. Создайте Web Service из папки `backend/`
3. Настройте переменные окружения
4. Создайте Background Workers для Celery

#### Вариант C: Heroku

1. Создайте Heroku приложение
2. Подключите PostgreSQL и Redis addons
3. Деплойте через Git
4. Настройте переменные окружения

### Шаг 3: Деплой фронтенда на Netlify

1. **Подключите репозиторий:**
   - Войдите в Netlify
   - Нажмите "New site from Git"
   - Выберите GitHub репозиторий

2. **Настройте сборку:**
   - Build command: `npm run build`
   - Publish directory: `dist`
   - Base directory: `frontend`

3. **Настройте переменные окружения:**
   - `VITE_API_URL` = URL вашего бэкенда

4. **Деплойте:**
   - Нажмите "Deploy site"
   - Дождитесь завершения сборки

### Шаг 4: Настройка домена

1. **В Netlify:**
   - Перейдите в Site settings > Domain management
   - Добавьте ваш кастомный домен (опционально)

2. **В бэкенде:**
   - Обновите `ALLOWED_HOSTS` с вашим Netlify доменом
   - Обновите `FRONTEND_*_URL` переменные

### Шаг 5: Настройка Telegram бота

1. Создайте бота через @BotFather
2. Получите токен
3. Добавьте токен в переменные окружения бэкенда
4. Настройте webhook (если используете)

## 🔧 Проверка работоспособности

### Фронтенд:
- [ ] Сайт загружается
- [ ] API запросы работают
- [ ] Аутентификация работает
- [ ] Все страницы доступны

### Бэкенд:
- [ ] API доступен по URL
- [ ] База данных подключена
- [ ] Redis работает
- [ ] Celery workers запущены
- [ ] Telegram бот отвечает

## 🐛 Устранение неполадок

### Частые проблемы:

1. **CORS ошибки:**
   - Проверьте `ALLOWED_HOSTS` в бэкенде
   - Убедитесь, что домен фронтенда добавлен

2. **API недоступен:**
   - Проверьте URL в `VITE_API_URL`
   - Убедитесь, что бэкенд запущен

3. **Ошибки базы данных:**
   - Проверьте `DATABASE_URL`
   - Убедитесь, что миграции выполнены

4. **Redis ошибки:**
   - Проверьте `REDIS_URL`
   - Убедитесь, что Redis доступен

## 📊 Мониторинг

### Рекомендуемые инструменты:
- **Uptime monitoring:** UptimeRobot, Pingdom
- **Error tracking:** Sentry
- **Analytics:** Google Analytics, Plausible
- **Performance:** Lighthouse, WebPageTest

## 🔒 Безопасность

1. **Обязательно измените:**
   - `SECRET_KEY` на уникальный
   - Все пароли и токены
   - Настройте HTTPS

2. **Рекомендации:**
   - Используйте сильные пароли
   - Регулярно обновляйте зависимости
   - Настройте мониторинг безопасности

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в панели управления платформы
2. Убедитесь, что все переменные окружения настроены
3. Проверьте статус внешних сервисов (база данных, Redis)

---

**Удачного деплоя! 🎉**
