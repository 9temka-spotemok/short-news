{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd backend && python -c \"import os; port=int(os.environ.get('PORT', 8000)); import uvicorn; uvicorn.run('main:app', host='0.0.0.0', port=port, log_level='info')\"",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 3
  }
}
