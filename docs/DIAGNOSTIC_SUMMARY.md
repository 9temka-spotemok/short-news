# 🔍 Краткий отчёт диагностики

## ✅ Что работает

1. **Инфраструктура:**
   - ✅ Redis: доступен (PONG)
   - ✅ PostgreSQL: работает (healthy)
   - ✅ Celery worker: запущен, слушает очереди `celery,analytics`
   - ✅ Backend API: работает на порту 8000
   - ✅ Celery задачи зарегистрированы: `recompute_company_analytics` доступна

2. **Данные:**
   - ✅ 131 компания в БД
   - ✅ 71 новость для компании `75eee989-a419-4220-bdc6-810c4854a1fe`
   - ✅ 71 новость с `priority_score` (обработаны частично)

## 🔴 Проблемы

### 1. Новости не полностью обработаны NLP

**Статус для компании `75eee989-a419-4220-bdc6-810c4854a1fe`:**
- ✅ `priority_score`: 71/71 (100%)
- ❌ `sentiment`: 0/71 (0%) — **КРИТИЧНО**
- ❌ `topic`: 0/71 (0%) — **КРИТИЧНО**

**Влияние:**
- Без `sentiment` нельзя вычислить `news_average_sentiment`
- Без `topic` нельзя правильно классифицировать новости
- Snapshot создастся, но с нулевыми/неправильными метриками

### 2. Нет change events

- 0 change events для компании
- Это нормально, если не отслеживаются изменения конкурентов
- Не критично для создания snapshot (будет 0 в метриках)

### 3. Нет snapshots

- 0 snapshots в БД для всех компаний
- Это ожидаемо, так как пересчёт не запускался или выполнялся с ошибками

## 🎯 Решение

### Шаг 1: Обработать новости NLP

Нужно запустить NLP обработку для добавления `sentiment` и `topic`:

```bash
# Проверить, есть ли Celery задача для NLP
docker exec shot-news-celery-worker celery -A app.celery_app inspect registered | grep nlp

# Если есть задача, запустить её
# Или использовать API endpoint для обработки новостей
```

### Шаг 2: Запустить пересчёт аналитики

После обработки новостей:

```bash
# Через API (с токеном авторизации)
curl -X POST "http://localhost:8000/api/v2/analytics/companies/75eee989-a419-4220-bdc6-810c4854a1fe/recompute?period=daily&lookback=60" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

Или через UI: нажать кнопку **"Recompute"** в ImpactPanel

### Шаг 3: Проверить результат

```bash
# Проверить создание snapshot
curl "http://localhost:8000/api/v2/analytics/companies/75eee989-a419-4220-bdc6-810c4854a1fe/impact/latest?period=daily" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 📊 Текущее состояние

| Компонент | Статус | Детали |
|-----------|--------|--------|
| Redis | ✅ Работает | PONG |
| PostgreSQL | ✅ Работает | Healthy |
| Celery Worker | ✅ Работает | Слушает analytics очередь |
| Новости | ⚠️ Частично | Есть priority_score, нет sentiment/topic |
| Change Events | ❌ Нет | 0 событий |
| Snapshots | ❌ Нет | 0 snapshots |

## 🚀 Следующие шаги

1. **СРОЧНО:** Обработать новости NLP (добавить sentiment и topic)
2. Запустить пересчёт аналитики через UI или API
3. Проверить создание snapshots
4. Обновить страницу в браузере

---

**Создано:** 2025-11-14  
**Компания для тестирования:** `75eee989-a419-4220-bdc6-810c4854a1fe`



