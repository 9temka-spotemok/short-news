# 🚀 Деплой проекта на Railway.app

## 📋 Предварительные требования

1. **Аккаунт Railway:** [railway.app](https://railway.app)
2. **GitHub репозиторий** с вашим проектом
3. **API ключи** (OpenAI, Telegram, Twitter, GitHub, Reddit)
4. **Домен Netlify** для фронтенда

## 🎯 Пошаговая инструкция

### Шаг 1: Подготовка проекта

Проект уже подготовлен к деплою на Railway:
- ✅ `railway.json` - конфигурация Railway
- ✅ `backend/env.production` - переменные окружения
- ✅ `requirements.txt` - зависимости Python
- ✅ Health endpoint `/api/v1/health`

### Шаг 2: Создание проекта на Railway

1. **Войдите в Railway:**
   - Зайдите на [railway.app](https://railway.app)
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий `short-news`

3. **Настройте деплой:**
   - Railway автоматически определит, что это Python проект
   - Выберите папку `backend/` как корневую директорию
   - Railway использует `railway.json` для конфигурации

### Шаг 3: Добавление базы данных

1. **Добавьте PostgreSQL:**
   - В проекте нажмите "+ New"
   - Выберите "Database" → "PostgreSQL"
   - Railway создаст базу данных и автоматически добавит `DATABASE_URL`

2. **Добавьте Redis:**
   - Нажмите "+ New" снова
   - Выберите "Database" → "Redis"
   - Railway создаст Redis и автоматически добавит `REDIS_URL`

### Шаг 4: Настройка переменных окружения

В Railway Dashboard → Variables добавьте:

#### Обязательные переменные:
```bash
# Application
ENVIRONMENT=production
DEBUG=false
SECRET_KEY=your-super-secret-production-key-here

# Frontend URLs (замените на ваш Netlify домен)
FRONTEND_BASE_URL=https://your-app-name.netlify.app
FRONTEND_SETTINGS_URL=https://your-app-name.netlify.app/settings
FRONTEND_DIGEST_SETTINGS_URL=https://your-app-name.netlify.app/settings/digest

# CORS (добавьте ваш Netlify домен)
ALLOWED_HOSTS=["https://your-app-name.netlify.app"]
```

#### API ключи (заполните реальными значениями):
```bash
# OpenAI API
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
TELEGRAM_CHANNEL_ID=@your_channel_name

# Twitter API (опционально)
TWITTER_API_KEY=your-twitter-api-key
TWITTER_API_SECRET=your-twitter-api-secret
TWITTER_ACCESS_TOKEN=your-twitter-access-token
TWITTER_ACCESS_TOKEN_SECRET=your-twitter-access-token-secret

# GitHub API (опционально)
GITHUB_TOKEN=your-github-token-here

# Reddit API (опционально)
REDDIT_CLIENT_ID=your-reddit-client-id
REDDIT_CLIENT_SECRET=your-reddit-client-secret

# Email (опционально)
SENDGRID_API_KEY=your-sendgrid-api-key
FROM_EMAIL=noreply@shot-news.com
```

### Шаг 5: Деплой

1. **Railway автоматически начнет деплой** после добавления переменных
2. **Дождитесь завершения сборки** (обычно 2-5 минут)
3. **Получите URL бэкенда** в разделе "Deployments"

### Шаг 6: Настройка миграций базы данных

После успешного деплоя нужно выполнить миграции:

1. **Откройте Railway CLI:**
   ```bash
   npm install -g @railway/cli
   railway login
   railway link
   ```

2. **Выполните миграции:**
   ```bash
   railway run alembic upgrade head
   ```

### Шаг 7: Обновление фронтенда

1. **Получите URL бэкенда** из Railway (например: `https://shot-news-backend.railway.app`)

2. **Обновите переменную в Netlify:**
   - Site settings → Environment variables
   - `VITE_API_URL` = `https://shot-news-backend.railway.app`

3. **Перезапустите деплой** в Netlify

## 🔧 Проверка работоспособности

### Проверьте бэкенд:
- [ ] Health endpoint: `https://your-backend-url.railway.app/api/v1/health`
- [ ] API docs: `https://your-backend-url.railway.app/docs`
- [ ] База данных подключена
- [ ] Redis работает

### Проверьте фронтенд:
- [ ] Сайт загружается на Netlify
- [ ] API запросы работают
- [ ] Нет ошибок в консоли браузера

## 🚨 Устранение неполадок

### Проблема: Ошибка сборки
**Решение:** Проверьте логи в Railway Dashboard → Deployments

### Проблема: База данных не подключена
**Решение:** Убедитесь, что `DATABASE_URL` правильно настроен

### Проблема: CORS ошибки
**Решение:** Проверьте `ALLOWED_HOSTS` и `FRONTEND_*_URL`

### Проблема: Telegram бот не работает
**Решение:** Проверьте `TELEGRAM_BOT_TOKEN`

## 📊 Мониторинг

Railway предоставляет:
- **Логи** в реальном времени
- **Метрики** использования
- **Health checks** автоматически
- **Автоматические перезапуски** при сбоях

## 💰 Стоимость

- **Railway Hobby Plan:** $5/месяц
- **PostgreSQL:** включен в план
- **Redis:** включен в план
- **Трафик:** включен в план

## 🎉 Готово!

После выполнения всех шагов:
- ✅ Бэкенд работает на Railway
- ✅ База данных настроена
- ✅ Redis работает
- ✅ Фронтенд подключается к бэкенду
- ✅ Весь функционал доступен

---

**🚀 Ваш проект успешно задеплоен на Railway!**
