# 🔧 ИСПРАВЛЕНИЕ ОШИБОК ПОДКЛЮЧЕНИЯ К API

## ❌ Проблема
```
Failed to load resource: net::ERR_CONNECTION_REFUSED
localhost:8000/api/v1/companies/?limit=200:1
localhost:8000/api/v1/news/categories/list:1
localhost:8000/api/v1/news/?limit=20:1
```

## 🎯 Причина
Фронтенд на Netlify пытается подключиться к `localhost:8000`, но там нет вашего бэкенда.

## ✅ Решение

### Шаг 1: Деплойте бэкенд
Сначала нужно задеплоить бэкенд на одну из платформ:

#### Вариант A: Railway.app (рекомендуется)
1. Зайдите на [railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Создайте PostgreSQL и Redis сервисы
4. Деплойте из папки `backend/`
5. Получите URL бэкенда (например: `https://shot-news-backend.railway.app`)

#### Вариант B: Render.com
1. Зайдите на [render.com](https://render.com)
2. Создайте Web Service из папки `backend/`
3. Получите URL бэкенда

#### Вариант C: Heroku
1. Создайте Heroku приложение
2. Деплойте бэкенд
3. Получите URL бэкенда

### Шаг 2: Настройте переменную окружения в Netlify
1. Зайдите в панель управления Netlify
2. Выберите ваш сайт
3. Перейдите в Site settings → Environment variables
4. Добавьте переменную:
   - **Key:** `VITE_API_URL`
   - **Value:** `https://your-backend-url.com` (замените на URL вашего бэкенда)

### Шаг 3: Перезапустите деплой
1. В Netlify нажмите "Trigger deploy" → "Deploy site"
2. Дождитесь завершения сборки

## 🔍 Проверка

После настройки проверьте:
- [ ] Сайт загружается без ошибок в консоли
- [ ] API запросы идут на правильный URL
- [ ] Данные загружаются

## 📋 Пример настройки

Если ваш бэкенд на Railway:
```
VITE_API_URL = https://shot-news-backend.railway.app
```

Если ваш бэкенд на Render:
```
VITE_API_URL = https://shot-news-backend.onrender.com
```

## ⚠️ Важно

- **Netlify** - только для фронтенда (статический сайт)
- **База данных** - нужна для бэкенда, не для Netlify
- **Бэкенд** - должен быть задеплоен отдельно

---

**🎯 Сначала задеплойте бэкенд, затем настройте VITE_API_URL в Netlify!**
