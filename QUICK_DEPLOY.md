# 🚀 Быстрый старт деплоя на Netlify

## ⚡ Экспресс-деплой (5 минут)

### 1. Подготовка фронтенда
```bash
# Установите зависимости
cd frontend
npm install

# Проверьте сборку
npm run build
```

### 2. Деплой на Netlify
1. Зайдите на [netlify.com](https://netlify.com)
2. Нажмите "New site from Git"
3. Выберите ваш GitHub репозиторий
4. Настройте:
   - **Build command:** `npm run build`
   - **Publish directory:** `frontend/dist`
   - **Base directory:** `frontend`
5. Нажмите "Deploy site"

### 3. Настройка переменных окружения
В Netlify Dashboard → Site settings → Environment variables:
```
VITE_API_URL = https://your-backend-url.com
```

### 4. Деплой бэкенда (выберите один вариант)

#### Railway.app (рекомендуется)
1. Зайдите на [railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Создайте PostgreSQL и Redis сервисы
4. Деплойте из папки `backend/`
5. Настройте переменные из `backend/env.production`

#### Render.com
1. Зайдите на [render.com](https://render.com)
2. Создайте Web Service из папки `backend/`
3. Настройте переменные окружения
4. Создайте Background Workers для Celery

### 5. Обновление URL
После деплоя бэкенда обновите в Netlify:
```
VITE_API_URL = https://your-backend-url.com
```

## 🔧 Проверка работоспособности

### Фронтенд
- [ ] Сайт загружается
- [ ] API запросы работают
- [ ] Нет ошибок в консоли

### Бэкенд
- [ ] API доступен
- [ ] База данных подключена
- [ ] Redis работает

## 📋 Чек-лист перед деплоем

- [ ] Все зависимости установлены
- [ ] Проект собирается без ошибок
- [ ] Переменные окружения настроены
- [ ] База данных и Redis доступны
- [ ] API ключи получены

## 🆘 Быстрая помощь

**Проблема:** CORS ошибки
**Решение:** Проверьте `ALLOWED_HOSTS` в бэкенде

**Проблема:** API недоступен
**Решение:** Проверьте `VITE_API_URL` в Netlify

**Проблема:** Ошибки сборки
**Решение:** Запустите `npm run build` локально

## 📞 Поддержка

- 📖 Подробная инструкция: `NETLIFY_DEPLOYMENT_GUIDE.md`
- 🔧 Переменные окружения: `DEPLOYMENT_ENV_VARS.md`
- 🧪 Тестирование: `test-deployment.ps1` (Windows) или `test-deployment.sh` (Linux/Mac)

---

**Удачного деплоя! 🎉**